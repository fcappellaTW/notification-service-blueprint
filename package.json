{
  "name": "notification-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "clean": "rimraf dist dist.zip",
    "build": "tsc",
    "package:prepare": "cpr package.json dist/ -o && cpr package-lock.json dist/ -o && npm install --prefix ./dist --omit=dev",
    "package:zip": "zip -r dist.zip dist",
    "package": "npm run clean && npm run build && npm run package:prepare && npm run package:zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.155",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.7.1",
    "aws-sdk-client-mock": "^4.1.0",
    "cpr": "^3.0.1",
    "jest": "^30.2.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.4.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.908.0",
    "zod": "^4.1.12"
  }
}
